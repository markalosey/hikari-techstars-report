/* DEPRECATED — v1 single-page controller.
 *
 * The preview is now a multi-page lab (index.html landing card-grid + pages/*.html). The shared
 * logic moved to assets/common.js and the per-page controllers assets/page-*.js
 * (page-hero, page-opportunity, page-footprint, page-targets, page-graph, page-techstars,
 * page-sports) plus assets/roster.js. This file is no longer imported by any page and is kept
 * only to preserve git history of the v1 hero+accordion+drawer controller. Safe to remove.
 *
 * See special_reports/README.md for the current file catalog.
 */
